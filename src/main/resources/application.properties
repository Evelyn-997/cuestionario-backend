# Server
server.port=${PORT:8080}
#8085 (local)

# App
spring.application.name=cuestionario

# Datasource (MySQL)
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:55655/railway?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
  #jdbc:mysql://localhost:3306/retorika_quiz?useSSL=false&serverTimezone=Europe/Madrid
spring.datasource.username=appuser 
spring.datasource.password=UnaPasswordFuerte123!
  #retorika_user (local)
#retorika_pass123!(local)
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true
SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.MySQLDialect

# SQL init (data.sql / schema.sql)
spring.sql.init.mode=always

# CORS (lista separada por comas)
app.cors.allowed-origins=http://localhost:4200,http://localhost:5173,https://cuestionario-estilos.vercel.app


